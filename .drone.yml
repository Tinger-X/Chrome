kind: pipeline
type: docker
name: chrome

steps:
  - name: remove
    image: python:3.8
    commands:
      - docker stop $(docker ps -qf name=Chrome)
      - docker rm $(docker ps -qf name=Chrome)
      - docker rmi $(docker images -q chrome)
      - docker-compose up -d